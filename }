
### 3. Configuration Requirements

Your project must export:

- `type`: String identifier for your data type
- `provider.ip`: Your server's IP address (whitelist protection)
- `provider.handler`: Function to process incoming data

### 4. Data Handling

The handler function should:
- Accept a Record<string, any> as input
- Transform data into a valid format with primitive values
- Return the processed data object

Example:
